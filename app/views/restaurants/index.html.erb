<h1>Restaurants</h1>
<ul>
  <% @restaurant.each do |restaurant| %>
    <%= link_to restaurant.name, restaurant_path(restaurant)%>  </br>
  <% end %>
  </br>
 <%= link_to 'Add a new task ➕', new_restaurant_path %>
</ul>